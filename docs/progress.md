# wjfcm-go 开发进度

## 2026-05-21

### 已完成

- 建立新版工作区 `wjfcm-go/`。
- 建立 Gin 后端基础骨架。
- 建立 Vue 3 + Vite 前端基础骨架。
- Gin 后端已接入：
  - 配置加载。
  - MySQL 连接。
  - `wjf_` 表前缀兼容。
  - CORS。
  - 统一 JSON 响应。
  - 健康检查接口。
  - 管理员登录。
  - JWT 管理端认证中间件。
  - 后台 Access Token + Refresh Token，支持刷新令牌接口。
  - 后台接口权限中间件初版，基于旧权限 URL 和角色权限进行 403 拦截。
  - 后台退出登录接口。
  - 当前管理员资料更新接口。
  - 当前管理员修改密码接口，校验原密码并写入 bcrypt 新密码。
  - bcrypt 密码校验兼容。
  - 文章列表、详情、新增、更新、软删除、恢复、彻底删除。
  - 前台文章列表/详情独立接口，只返回已发布文章，详情访问增加点击量。
  - 文章详情和编辑接口会解码历史 `htmlspecialchars` 正文，兼容老数据展示。
  - 分类列表、新增、更新、删除。
  - 标签列表、新增、更新、删除。
  - 评论列表、审核内容更新、软删除、恢复、彻底删除。
  - 用户列表、软删除。
  - 用户资料更新。
  - 管理员列表、新增、更新、软删除。
  - 角色列表、新增、更新、软删除。
  - 角色权限绑定读取与保存，写入旧 Spatie `role_has_permissions` 表。
  - 权限列表、新增、更新、软删除。
  - 后台菜单树读取。
  - 后台菜单按当前管理员角色权限过滤，管理员 ID 1 作为超级管理员。
  - 导航、友情链接、闲言碎语、系统配置接口。
  - 前台评论列表接口，支持一级评论和二级回复。
  - 前台评论提交接口，已切换为用户登录后提交。
  - 前台用户注册、登录、资料读取、资料更新接口。
  - 前台邮箱验证码接口，支持注册验证和找回密码验证。
  - 前台忘记密码重置接口，兼容 bcrypt 新密码写入。
  - 前台用户头像上传接口。
  - 本地图片上传接口，兼容 `file`、`base64_img`、`editormd-image-file` 字段。
  - `/uploads` 静态文件访问。
  - `/images` 与 `/favicon.ico` 静态文件访问，兼容旧 public 资源。
  - 文章删除前检查评论，保持旧版行为。
  - 文章、评论、分类、标签、用户、管理员、角色、权限、导航、友情链接、闲言碎语回收站支持恢复和彻底删除接口。
  - 列表接口返回 `deleted_at`，前端可识别回收站数据状态。
- Vue 前端已接入：
  - 前台布局。
  - 前台文章列表。
  - 前台文章详情。
  - 后台登录页。
  - 后台基础布局。
  - 后台侧边栏读取后端权限菜单，不再在接口成功但无权限时回退展示全量菜单。
  - 后台右上角新增个人资料、修改密码、退出登录操作。
  - 后台请求遇到 Access Token 过期时自动刷新并重试一次。
  - 后台文章列表。
  - 后台文章新增/编辑页。
  - 文章封面上传。
  - 文章 Markdown 轻量编辑、HTML 预览、正文图片插入。
  - 后台评论、分类、标签、用户、管理员、角色、权限、导航、友情链接、闲言碎语、系统配置通用列表页。
  - 通用列表页支持按模块配置新增、编辑、删除。
  - 通用列表页和文章列表按当前管理员权限控制新增、编辑、删除按钮展示。
  - 文章列表和通用资源列表在回收站中支持恢复、彻底删除，并沿用统一确认弹窗和成功/失败提示。
  - 文章列表和通用资源列表支持多选批量删除、批量恢复、批量彻底删除。
  - 通用后台列表支持开关类快捷操作：评论审核、管理员状态、角色状态、权限菜单显示、友链状态、系统配置状态。
  - 分类、导航、权限列表支持树形层级展示、行内添加子级；分类/导航/权限表单的上级字段改为下拉选择。
  - 分类列表支持快捷进入该分类文章列表；分类、导航、权限排序字段支持行内编辑并自动保存。
  - 后台用户列表支持新增用户；管理员和用户列表支持独立密码修改弹窗。
  - 文章列表支持百家号推送：已发布且未推送文章可一键推送，成功后标记 `is_baijiahao`。
  - 管理员编辑时密码留空不修改。
  - 角色编辑支持权限树勾选并保存绑定。
  - 前台布局升级：固定顶部导航、分类导航、前台菜单、页脚系统配置。
  - 前台首页升级：置顶图文、文章列表、浏览量、关键词、侧栏。
  - 前台详情页升级：文章元信息、标签、简介、正文、侧栏。
  - 前台分类/标签/搜索列表补数字分页、阅读原文、空状态；标签页补 3 列图片卡片布局。
  - 前台文章详情支持上一篇/下一篇跳转。
  - 前台文章详情补转载说明和原文链接。
  - 前台补动态 SEO 工具，支持运行时 title/meta/OG/Twitter/canonical 更新。
  - 前台文章详情补 Article JSON-LD 结构化数据。
  - 前台 SEO 改为 Gin 服务端渲染：输出首页、分类、标签、搜索、归档、有些话、文章详情 HTML，并提供 `robots.txt`、`sitemap.xml`。
  - Gin 前台公共模板补站点验证 meta、统计代码、Google Adsense 和 PageAgent 加载。
  - 已删除 Vue 中被 Gin 模板替代的公开前台页面：`HomeIndex.vue`、`ArticleDetail.vue`、`ArticleListPage.vue`、`ArchivePage.vue`、`ChatPage.vue`、`HomeSidebar.vue`、`CommentBoard.vue`。
  - 前台布局支持站点验证 meta、统计代码 `site_tongji` 和配置型 Google Adsense 脚本加载。
  - 前台页脚补回版权年限、备案链接、转载声明和举报邮箱。
  - 前台补微信分享 JS 加载和配置入口，支持按系统配置更新分享标题、描述、链接和图标。
  - 前台侧栏：关于本站、搜索、标签云、热度榜、友情链接。
  - 前台侧栏已移除“历史上的今天”，不再依赖外部历史事件接口。
  - 前台侧栏补回公众号展示，并支持配置驱动的侧栏广告位。
  - 后台权限管理补自动层级、绑定校验；导航管理补回 `target` 和 `icon` 旧字段。
  - 后台补回微信关键词回复管理，支持搜索、新增、编辑、审核开关和排序；该旧表无 `deleted_at` 字段，已按普通删除兼容。
  - 后台系统配置补回旧版分组体验，支持基本信息、联系方式、SEO、微信、小程序分组筛选。
  - 微信服务端入口 `/wechat` 已补服务器校验、关注事件回复、文本关键词回复、QQ AI / 图灵文本兜底，以及图片/语音的图灵兜底。
  - 后台系统配置补回微信配置：公众号二维码、客服微信、微信收款码、公众号 AppID/Secret/Token/AESKey、微信网页授权、图灵和 QQ AI 配置；微信服务优先读取后台配置，缺失时回退 `.env`。
  - 前台文章详情评论区：评论列表、更多加载、一级评论提交、回复提交。
  - 前台文章详情评论区支持登录用户点赞/踩评论，并实时更新计数。
  - 前台文章详情评论区已改为 Gin 模板 + 原生 JS，支持自适应、表情、盖楼回复、点赞/踩。
  - 前台登录、注册、找回密码、个人中心已改为 Gin 模板 + 原生 JS。
  - 前台第三方 OAuth 回调路由已显式指向回调处理，避免回调链路被当成授权跳转处理。
  - 前台注册页接入邮箱验证码。
  - 前台用户中心修改邮箱时要求邮箱验证码，并防止邮箱重复占用。
  - 前台登录/注册/找回密码补图形验证码；注册页补确认密码校验。
  - 前台找回密码页面，支持邮箱验证码重置密码。
  - 已删除 Vue 中剩余前台用户页面和 `homeAuth` store/API，Vue 只保留后台管理。
  - 新版入口补回旧布局里的 `page-agent` 脚本。
  - 新增逐页迁移验收清单 `docs/page-migration-checklist.md`。
  - 前台分类页、标签页、搜索页、归档页、有些话页面独立路由和基础 UI。
  - 前台搜索页支持关键词高亮展示，标题和简介命中词会标记出来。
  - 前台文章列表接口支持按标签名称过滤。
  - 前台新增归档接口 `GET /api/home/archive`，按旧页面逻辑基于分类聚合文章。
  - 前台公共布局补回旧版右侧工具条、收起展开、用户快捷入口、QQ、微信、打赏和返回顶部。
  - Gin 前台公共模板补右侧工具条、公众号、配置广告位和申请友链表单，支持移动端自适应。
  - Gin 前台模板重新同步 Vue 版已修复体验：置顶文章独立展示、导航只保留当前项选中、搜索关键词高亮、数字分页、代码块复制按钮、评论表情自动收起和盖楼回复上下文。
  - 前台 H5 样式继续加固：分类/搜索文章列表改为紧凑信息流、置顶推荐横向滑动、热度榜长标题不溢出、归档列表不撑宽、文章详情代码块不撑开页面。
  - 前台文章详情补回正文/评论图片点击放大预览，支持遮罩关闭、ESC、左右切换和查看原图；同时补回上一篇/下一篇文章导航。
  - 服务端配置结构同步旧版 `.env.example`，补齐邮件、Redis、缓存/队列/session、百家号、百度站长、图灵、QQ AI、微信网页授权、AWS、Pusher 等配置项。
  - 旧版百度站长主动推送已迁移：兼容 `/tools/linkSubmit`，并新增后台认证接口 `POST /api/admin/tools/baidu-submit`。
  - 后台评论批量替换已补回：评论列表新增“批量替换”，后端支持 `POST /api/admin/comments/replace`。
  - 后台文章批量替换已补回：文章列表新增“批量替换”，后端支持 `POST /api/admin/articles/replace`，可选择替换标题、简介、Markdown、HTML 正文。
  - 项目目录已统一为 `wjfcm-go/`，旧临时目录 `gin-vue/` 已删除。
  - `public/` 已迁入 `wjfcm-go/public`，`PUBLIC_DIR` 改为独立项目默认的 `../public`。
  - Gin 已支持 `wjfcm-go/public` 下根目录文件直出，例如 `/ads.txt`、`/bdunion.txt`、`/google*.html`，且不影响 `/article/:id`、`/category/:id` 等动态路由。
  - 当前迁移上下文已固化到 `docs/migration.md` 和 `docs/standalone-context.md`。
  - `wjfcm-go/public` 已清理旧 Laravel/Layui 构建产物，仅保留根验证文件、`images/` 和 `uploads/`。
  - 旧版 `/blank` 错误页已补 Gin 模板兼容入口。
  - 旧版百家号加密回调 `/baidu/serve` 已补 Gin 兼容入口，支持签名校验和 AES 解密输出。
  - 前台公共头部和文章评论区已同步登录态，登录后显示用户信息并隐藏评论区登录按钮。
  - 新增 CMS 初始化安装向导 `/install`：支持数据库配置、自动建库建表、初始化超级管理员/角色/菜单/基础配置、生成 `.env` 和 `.install.lock`。

### 已验证

- `wjfcm-go/server` 已执行 `go mod tidy`。
- `wjfcm-go/server` 已执行 `go test ./...`，所有 Go 包通过编译检查。
- `wjfcm-go/server` 临时启动 `APP_PORT=18080`，已检查首页、文章详情、搜索页、有些话页面均返回 200，并确认置顶区、代码复制、表情、回复上下文、搜索高亮模板标记存在。

### 当前限制

- 第三方 OAuth 代码已完成，仍需要在 GitHub、QQ、微博平台申请真实应用，并把回调地址写入 `.env` 后才能线上验证。
- `wjfcm-go/web` 已执行 `npm run build`，生产构建通过。
- 已用文件哈希核对确认旧临时目录与正式目录内容一致后删除 `gin-vue/`。

### 下一步

1. 按 `docs/page-migration-checklist.md` 做最终人工验收，重点看旧后台操作习惯、前台移动端细节和真实数据边界。
2. 做线上联调：OAuth 平台回调、微信公众号服务器 URL、邮件验证码真实 SMTP、百家号发布/回调、百度站长主动推送。
3. 准备生产部署：确认 `.env`、数据库备份、静态资源目录、上传目录权限、反向代理、HTTPS 和进程守护。
4. 上线前做一次全量冒烟：前台 SEO 页面、登录注册找回密码、评论/回复/点赞、后台 CRUD、上传、微信关键词、站点地图。
5. 旧调试入口 `/tools/tuling`、`/admin/test/*` 不迁移到生产新版，若后续需要调试能力，建议单独做受权限保护的诊断页面。
